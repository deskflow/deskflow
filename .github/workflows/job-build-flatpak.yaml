name: Ubuntu-flatpak

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

jobs:
  flatpak:
    runs-on: ubuntu-latest
    container:
      image: bilelmoussaoui/flatpak-github-action:kde-5.15-21.08
    env:
      GIT_COMMIT: ${{ github.sha }}
    name: ubuntu-flatpak

    steps:
      - uses: actions/checkout@v2
        with:
          submodules: 'recursive'

      - uses: bilelmoussaoui/flatpak-github-actions/flatpak-builder@v4
        name: "Build"
        with:
          bundle: synergy.flatpak
          manifest-path: com.symless.Synergy.yml
