# Runs

name: "Lint CMake files"
on:
  pull_request:

jobs:
  lint-cmake-files:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install dependencies
        run: pip install cmake_format

      - name: Lint CMake files
        run: ./scripts/lint_cmake_files.sh
